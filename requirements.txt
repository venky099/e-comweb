# ---------------------------------------------------------------------------
# Application dependencies.
#
# Version ranges rather than exact pins, so a fresh install picks up wheels
# built for whichever Python you are on (3.11 through 3.13). Upper bounds stop
# at the next major version, where breaking changes are expected.
#
# For a byte-identical environment (CI, production, handing the project to
# someone else), install the lockfile instead:
#
#     pip install -r requirements.lock.txt
#
# Regenerate it after changing anything here:
#
#     pip install -r requirements.txt --upgrade
#     pip freeze | sort > requirements.lock.txt
#
# Supported Python: 3.11 - 3.13. Django 5.2 LTS does not support 3.14.
# ---------------------------------------------------------------------------

# ---- Core ----
Django>=5.2,<6.0            # 5.2 is the LTS line; 6.0 changes APIs
Pillow>=11.0                # unbounded: newest release has wheels for new Pythons
django-environ>=0.12,<1.0

# PostgreSQL driver. Not needed when running on SQLite (USE_SQLITE=True) --
# comment it out if the install fails and you only need a local dev run.
psycopg2-binary>=2.9.9,<3.0

# ---- REST API layer ----
djangorestframework>=3.16,<4.0
djangorestframework-simplejwt>=5.5,<6.0
django-filter>=25.1
drf-spectacular>=0.28,<1.0
django-cors-headers>=4.7,<5.0

# ---- Forms / UI ----
django-crispy-forms>=2.4,<3.0
crispy-bootstrap5>=2024.2

# ---- Security / throttling ----
django-ratelimit>=4.1,<5.0

# ---- Caching (optional; falls back to in-memory when REDIS_URL is unset) ----
redis>=6.0,<7.0
django-redis>=6.0,<7.0

# ---- Payments ----
razorpay>=1.4,<2.0
stripe>=12.0

# ---- Deployment ----
gunicorn>=23.0,<24.0        # not used on Windows; harmless to install
whitenoise>=6.11,<7.0
django-storages>=1.14,<2.0
boto3>=1.34
